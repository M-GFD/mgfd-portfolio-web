# Copia este archivo a .env en la raíz solo si quieres un único lugar de referencia.
# Los servicios usan sus propios .env (ver apps/api/.env.example, apps/web/.env.example, services/ml/.env.example).

DATABASE_URL=postgresql://meliora:meliora@localhost:5432/meliora
REDIS_URL=redis://localhost:6379
JWT_SECRET=change-me-in-development
ML_SERVICE_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
